# Normalize line endings; the repo is LF.
* text=auto eol=lf

# Binary assets — never diff/merge them.
*.png binary
*.moc3 binary
*.can3 binary
*.cmo3 binary
*.mp3 binary
*.wav binary

# Lockfiles are generated, keep them diffable but do not merge automagically.
bun.lock text eol=lf
